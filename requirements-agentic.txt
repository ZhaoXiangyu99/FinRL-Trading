# Install after the CUDA-enabled PyTorch build supplied by the GPU host.
# These are intentionally separate from the core FinRL requirements.
transformers>=5.0.0,<6
accelerate>=1.10.0,<2
datasets>=4.0.0,<5
peft>=0.17.0,<1
trl>=0.27.0,<1
bitsandbytes>=0.49.0,<1
safetensors>=0.6.0,<1
